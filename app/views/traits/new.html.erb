<div class="form">
	<%= form_for [@character, @trait] do |f| %>
	  <%= f.label :description %>
	  <%= f.text_field :description %>

	  <%= f.submit "Create Trait", :class => "offset" %>
	<% end %>
</div>